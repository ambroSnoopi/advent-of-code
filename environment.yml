# environment.yml

name: AoC
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11.10
  - pip:
      - numpy==2.1.3
      - pandas==2.2.3
      - pip==24.3.1
      - pytz==2024.2
      - tqdm==4.67.1
      - tzdata==2024.2
prefix: ./env
